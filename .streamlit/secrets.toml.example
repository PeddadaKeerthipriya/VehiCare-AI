# Copy this file to .streamlit/secrets.toml for local runs,
# or paste these keys into the "Secrets" panel in Streamlit
# Community Cloud (Settings -> Secrets) when you deploy.
# Never commit the real .streamlit/secrets.toml file.

SUPABASE_URL = "https://YOUR-PROJECT.supabase.co"
SUPABASE_ANON_KEY = "your-supabase-anon-key"

# Backend is already deployed on Render.
FASTAPI_URL = "https://vehicare-ai.onrender.com"
